<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Comment Index</title>
  </head>
  <body>
    <h1>Comment Index</h1>
    <% for(comment in comments){%> <% let currentComment = comments[comment] %>
    <p><b> <%= currentComment.username%> says: </b> <%= currentComment.comment %></p>
    <div style="display: flex">
      <form action="/comments/<%= comment.id %>/edit?_method=PATCH " method="GET">
        <button disabled>Edit</button>
      </form>

      <form action="/comments/<%= comment.id %>/delete?_method=DELETE " method="POST">
        <button disabled>Delete</button>
      </form>
    </div>
    <% } %>
  </body>
</html>
